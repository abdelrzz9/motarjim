version: "3.9"

services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ../apps/web/src:/app/apps/web/src
      - ../crates:/app/crates
    command: npm run dev --prefix /app/apps/web
